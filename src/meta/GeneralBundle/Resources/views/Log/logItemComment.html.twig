<div class="avatar">
  {% if user %}
  <img src="{{ user.avatar }}" width="30" height="30" class="avatar"/>
  {% else %}
  <img src="{{ asset("/bundles/metageneral/img/defaults/user.png") }}" width="30" height="30" class="avatar"/>
  {% endif %}
</div>
<div class="comment">
  <header>
    {% if user %}
    <a href="{{ path('u_show_user_profile', {'username': user.username}) }}">{{ user.firstName }}</a>
    {% else %}<span class="muted">Deleted user</span>{% endif %}
     — <span>{{ date|date("d/m/Y G:i") }}</span></header>
  <div>{{ text|deeplinks|markdown }}</div>
</div>