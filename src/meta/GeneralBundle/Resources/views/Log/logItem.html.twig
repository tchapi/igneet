<span class="icon-{{ icon }} icon-large"></span> — 
<span class="label fullDate">{{ date|date("d/m/Y") }}</span> <span class="label label-info">{{ date|date("H:i") }}</span>
<span class="avatar">
  {% if not user.isDeleted %}
  <img src="{{ user.avatar }}" width="18" height="18" class="avatar"/>
  {% else %}
  <img src="{{ asset("/bundles/metageneral/img/defaults/user.png") }}" width="18" height="18" class="avatar"/>
  {% endif %}
</span> — 
<span>{{ text|raw }}</span>
{% if combinedCount > 1 %}
<span>({{ combinedCount }})</span>
{% endif %}