<div class="pull-right">
  <ul class="nav pull-right">
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          <img src="{{ asset('bundles/metageneral/img/langs/' ~ user.getPreferredLanguage() ~ '.png') }}" width="24px" height="24px" title="{{ available_languages[user.getPreferredLanguage()].name }}"/>
        </a>
        <ul class="dropdown-menu">
            {% for code, language in available_languages %}
              <li><a href="{{ path('g_switch_language', { 'locale': code } ) }}"><img src="{{ asset('bundles/metageneral/img/langs/' ~ code ~ '.png') }}" width="24px" height="24px"/> {{ language.name }}</a></li>
            {% endfor %}
        </ul>
      </li>
  </ul>
</div>
